<b>Cart</b>


<ul>
    {{#each quantity}}
        <li>{{this.product.name}} - {{this.product.description}} - {{this.quantity}}<button>Remove From Cart</button></li>
    {{else}}
        No results
    {{/each}}
</ul>